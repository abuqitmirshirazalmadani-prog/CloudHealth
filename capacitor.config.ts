import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cloudhealth.app',
  appName: 'CloudHealth',
  webDir: 'dist/app/browser'
};

export default config;
